/**
 * Here will be defined all the wrapper functions, middlewares & factories used by the controllers, routes & schemas
 */

const schemaFactory = () => {};
const catchAsync = () => {};
const validator = () => {};
